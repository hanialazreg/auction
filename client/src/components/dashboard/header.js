import React, { Component } from "react";

export default class Header extends Component {
  render() {
    return (
      <div>
        <h1>here is the header of the admin dashboard</h1>
      </div>
    );
  }
}
